########################################################################################################################
# EventConfig
########################################################################################################################
gav:
  groupId: io.inugami
  artifactId: basic-plugin
  version: 4.0.0

name: events-test
scheduler: "0 0/5 * * * ?"
enable: true

#=======================================================================================================================
# SimpleEvents
#=======================================================================================================================
#simpleEvents:
#  - name: cpu
#    provider: SystemCpuProvider
#    scheduler: 0/10 * * * * ?

#=======================================================================================================================
# Events
#=======================================================================================================================
events:
  #---------------------------------------------------------------------------------------------------------------------
  # foobar-api-pourcentrage
  #---------------------------------------------------------------------------------------------------------------------
  - name: cpu-multi
    provider: SystemCpuProvider
    scheduler: 0/15 * * * * ?
    targets:
      - name: cpu-multi-target1
      - name: cpu-multi-target2

      #- name: gravida-pourcentrage
      #  query: >
      #   summarize(asPercent(sumSeries(org.foo.bar.jmx.gravida.sessions),sumSeries(org.foo.bar.jmx.*.session)), "24h", "avg",true)
